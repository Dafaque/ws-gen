import 'package:msgpack_dart/msgpack_dart.dart';

class encoder {
  static Uint8List encode(Encodable e) => serialize(e.topMap());
  static Map<String, dynamic> decode(Uint8List d) => (deserialize(data) as Map<dynamic, dynamic>)
          .map((key, value) => MapEntry(key as String, value));;
}